# Practice Session - 2026-08-02

## Concept Practiced
- Concept: 栈 | Difficulty: 🟢 Beginner | Exercise: 用 list 实现 MyStack + 括号匹配 is_balanced

## User's Submitted Code
```python
class MyStack:
    """基于 Python list 的栈,栈顶在列表末尾。"""

    def __init__(self):
        # TODO: 初始化内部存储
        self._stack = []

    def push(self, x):
        # TODO: 将 x 压入栈顶
        self._stack.append(x)

    def pop(self):
        # TODO: 弹出并返回栈顶元素;空栈返回 None
        if self.is_empty():
            return None
        return self._stack.pop()

    def top(self):
        # TODO: 返回栈顶元素但不弹出;空栈返回 None
        if self.is_empty():
            return None
        return self._stack[-1]

    def is_empty(self):
        # TODO: 栈为空返回 True
        return not self._stack

    def size(self):
        # TODO: 返回元素个数
        return len(self._stack)

    def __repr__(self):
        return f"MyStack({self._stack})"


def is_balanced(s: str) -> bool:
    """判断括号串 s 是否匹配,匹配返回 True。"""
    pattern = {")": "(", "}": "{", "]": "["}
    front = ('(', '[', '{')
    back = (')', ']', '}')

    if not s:
        return True
    stack = MyStack()
    for ch in s:
        if ch in front:
            stack.push(ch)
        elif ch in back:
            if stack.is_empty() or stack.top() != pattern[ch]:
                return False
            stack.pop()
    return stack.is_empty()
```

## AI Feedback

**做得好的地方:**
- `MyStack` 五个方法全部正确,空栈时 `pop`/`top` 返回 `None` 的边界处理到位
- 用字典 `pattern` 映射右括号 → 左括号,避免多个 `if`,代码干净
- 循环内匹配逻辑完全正确:左括号入栈、右括号比对栈顶、不匹配立即返回 `False`
- 额外写了 `__repr__` 便于调试

**发现的问题(Socratic 引导):**
- 第一版最后一行是 `return not stack`。`MyStack` 实例未定义 `__bool__` 或 `__len__`,Python 真值测试默认恒为真,所以 `not stack` 恒为 `False`,导致所有平衡串被判为不匹配(测试在 `'()'` 处失败)。
- 知识点:Python 对象真值测试优先级为 `__bool__()` → `__len__()`(非 0 为真)→ 默认真。修复为 `return stack.is_empty()` 即可。

**边界情况确认:**
- 空串 `""` 判为匹配 ✓(提前返回 True)
- 空栈时遇到右括号 → 立即返回 False ✓(如 `")"`、`"[}]"`)
- 遍历结束栈不为空 → 返回 False ✓(如 `"("`、`"({})["`)

**编码环境问题:**
- Windows 控制台默认 GBK 编码,`print("✅")` 触发 `UnicodeEncodeError`。已在脚本头部添加 `sys.stdout.reconfigure(encoding="utf-8")` 兼容,或可用 `PYTHONUTF8=1 python starter.py` 运行。

**代码质量建议(后续练习可尝试):**
- 给 `MyStack` 定义 `__len__`,让 `len(stack)` / 真值测试更自然
- `is_balanced` 可用 `collections.deque` 或纯 list 实现,但复用 `MyStack` 更符合本练习目标

## Assessment
- Understanding: Good
- Status: in_progress → in_progress | Confidence: 0.1 → 0.25
